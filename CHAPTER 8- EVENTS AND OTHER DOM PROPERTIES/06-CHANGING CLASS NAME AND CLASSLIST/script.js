// we have the class written in html is red green text
//  we  can chang the class by using id

// first.className="red text"
// first.className="green"

// we can remove the class by:
// first.classList.remove("green")

// we can add the class by:
// first.classList.add("red")

// to check whether the class present or not wer can:-
// first.classList.contains('red')


// EXAMPLE:-

// changing the clas using class name
// first.className="green text"

// // getting the class name:-
// first.classList

// // removing the class using:-
// // first.classList.remove("green")

// // adding the class using :-
// first.classList.add("blue")

// // removing the class by using:-
// first.classList.remove("blue")

// // to used the class if not used and if used to not let that class use in our webpage we use:-
// first.classList.toggle("red")

// // to check whether your class is is present or not 
// first.classlist.contains("blue")

// let a=document.getElementById("first")
// first.className="red text"
// // console.log(first.classList)
// // first.classList.remove("red")
// // first.classList.add("blue")
// // first.classList.toggle("green")
// console.log(first.classList.contains("blue"))


// let a=document.getElementById("first")
// a.className="text"
// // console.log(a.classList)
// a.classList.add("blue")
// a.classList.toggle("red")
// a.classList.remove("blue")
// console.log(a.classList.contains("blue"))