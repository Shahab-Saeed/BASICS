// first.insertAdjacentHTML("beforebegin","<h1> beforebegin</h1>") // this will added before the tag(div here)
// first.insertAdjacentHTML("beforeend","<h1> beforeend</h1>")// this will be added after the div statement but inside the div tag(here)
// first.insertAdjacentHTML("afterbegin","<h1>afterbegin</h1>")// this will be added before the div statement but inside the div tag(here)
// first.insertAdjacentHTML("afterend","<h1>afterend</h1>")// this will be added aftyer the div statement but outside the div tag(here)


// first.insertAdjacentHTML("beforebegin","<h1>before begin</h1>")
// first.insertAdjacentHTML("beforeend","<h1>before end</h1>")
// first.insertAdjacentHTML("afterend","<h1>after end</h1>")

// let first=document.getElementById("first")
// first.insertAdjacentHTML("beforebegin","<p><b>this is before begin</b></p>")
// first.insertAdjacentHTML("beforeend","<p><b>this is before end</b></p>")
// first.insertAdjacentHTML("afterbegin","<p><b>this is after begin</b></p>")
// first.insertAdjacentHTML("afterend","<p><b>this is after end</b></p>")


// NODE REMOVAL:-

// we can remove any node(text,element,comment)
// let first=document.getElementById("first")
// first.remove()

